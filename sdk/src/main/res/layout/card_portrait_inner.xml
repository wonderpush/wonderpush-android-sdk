<?xml version="1.0" encoding="utf-8"?>
<com.wonderpush.sdk.inappmessaging.display.internal.layout.CardLayoutPortrait xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:iam="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_content_root"
    style="@style/WPIamUI.Card"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:theme="@style/WPIamUI"
    iam:maxHeightPct="@dimen/dialogHeightPct"
    iam:maxWidthPct="@dimen/dialogWidthPct">

    <ImageView
        android:id="@+id/image_view"
        style="@style/WPIamUI.Card.ImageView"
        tools:src="@drawable/placeholder_850_850" />

    <TextView
        android:id="@+id/message_title"
        style="@style/WPIamUI.Text.Title"
        android:paddingLeft="@dimen/paddingSmall"
        android:paddingTop="@dimen/paddingSmall"
        android:paddingRight="@dimen/paddingSmall"
        android:singleLine="false"
        android:maxLines="3"
        tools:text="Your wishlist items are on sale!!" />

    <ScrollView
        android:id="@+id/body_scroll"
        style="@style/WPIamUI.Card.Scroll">

        <TextView
            android:id="@+id/message_body"
            style="@style/WPIamUI.ModalBody"
            tools:text="@string/placeholder_very_short" />
    </ScrollView>

    <LinearLayout
        android:id="@+id/action_bar"
        style="@style/WPIamUI.Card.ActionBar">

        <Button
            android:id="@+id/secondary_button"
            style="@style/WPIamUI.Card.ActionBar.Button"
            tools:text="Secondary"
            tools:textColor="#000000" />

        <Button
            android:id="@+id/primary_button"
            style="@style/WPIamUI.Card.ActionBar.Button"
            tools:text="Primary"
            tools:textColor="#000000" />
    </LinearLayout>

</com.wonderpush.sdk.inappmessaging.display.internal.layout.CardLayoutPortrait>
